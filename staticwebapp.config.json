{
    "routes": [
        {
            "route": "/authIndex.html",
            "allowedRoles": ["authenticated"]
        },
        {
            "route": "/.auth/login/github",
            "redirect": "/authIndex"
        }
    ]
}